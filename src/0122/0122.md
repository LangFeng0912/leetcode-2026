- [1877. 数组中最大数对和的最小值](https://leetcode.cn/problems/minimize-maximum-pair-sum-in-array/description/)
    - 快速排序：
        - 选择一个pivot，随后递归，左边是比pivot小的，右侧是比pivot大的，随后继续递归左边，右边
        - 注意可以不用空间复杂度
        ```
        （待排序的数组， 数组的左边界(例如，从起始位置开始排序，则l=0)， 数组的右边界(例如，排序截至到数组末尾，则r=a.length-1)
        ```
        - quickSort:
            - partition
            - quickSort(左)
            - quickSort（右）
