- 回溯问题 （递归 + 剪枝）
    - backtrace
        - 终止条件； res + path; return
        - 遍历下一个可能 for next:
            - path.add(next);
            - backtrace next
            - path.removeLast();

    - 字符串转化:
        - StringBuilder str = new StringBuilder();
        - str.append();
        - str.deleterCharAt(index);
        - str.toString();

    - 初始化Arryalist： Arrays.asList("a", "b", "c")
    - char to String : `String.valueOf(charA)`